spring:
  application:
    name: todo_ms
  datasource:
    url: jdbc:postgresql://localhost:5432/todo_db
    username: postgres
    password: senh@BD
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
    properties:
      hibernate.format_sql: true
